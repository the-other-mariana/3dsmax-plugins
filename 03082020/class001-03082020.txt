3ds max 2020

edit poly (select bola) es un modificador

shift y arrastras para copiar (copia es aparte)

$ seleccionado $Box001

units setup > system unit setup

select y ctrl E


--s = $Teapot001

--scale s [1, 2, 3]
-- vete a front i alt y luego perspective en la esquina
contador = 1
for x in $ do
(
	contador += 1
	x.scale.x = contador
	x.scale.y = contador
	x.scale.z = contador
	move [0,2* contador,0]
)
####

script spot get help and codes


function saludo = 
(
	--print "hola"
	Messagebox "hola"
)

saludo()
